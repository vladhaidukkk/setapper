import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';

ReactDOM.render(
  <React.StrictMode>
    <h1 className="bg-red-500">Jjsh</h1>
  </React.StrictMode>,
  document.getElementById('root')
);
